# Cognix MVP 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 完成 Cognix MVP 版本开发，支持用户通过 CLI 配置周报规则，到点能收到提醒并生成指定格式的周报，同时完成飞书 Bot 基础接入。

**架构：** 采用模块化分层设计，核心层与交互层解耦：核心层实现偏好存储、规则管理、调度触发能力；交互层提供 CLI 和飞书 Bot 两个入口；数据存储采用 SQLite + JSON 文件混合方案，无外部依赖，可本地运行。

**技术栈：** Python 3.10+ / Click（CLI） / APScheduler（调度） / lark-oapi-sdk（飞书接入） / SQLite（存储）

***

## 项目文件结构

```
cognix/
├── __init__.py                 # 包入口
├── core/                       # 核心模块
│   ├── __init__.py
│   ├── preference_store.py     # 偏好存储模块
│   ├── rule_engine.py          # 规则引擎模块
│   ├── scheduler.py            # 调度触发模块
│   └── event_collector.py      # 事件采集模块
├── interfaces/                 # 交互层
│   ├── __init__.py
│   ├── cli/                    # CLI 入口
│   │   ├── __init__.py
│   │   └── main.py
│   └── feishu/                 # 飞书 Bot 入口
│       ├── __init__.py
│       └── bot.py
├── storage/                    # 存储层
│   ├── __init__.py
│   ├── sqlite_store.py         # SQLite 存储实现
│   └── file_store.py           # JSON 文件存储实现
└── utils/                      # 工具类
    ├── __init__.py
    └── config.py               # 配置管理
```

***

## 任务列表

### 任务 1：项目初始化与基础配置

**文件：**

- 创建：`pyproject.toml`
- 创建：`README.md`
- 创建：`cognix/__init__.py`
- 创建：`cognix/utils/config.py`
- [ ] **步骤 1：编写 pyproject.toml 项目配置**

```toml
[tool.poetry]
name = "cognix"
version = "0.1.0"
description = "面向企业 AI Agent 的个人工作习惯与偏好记忆引擎"
authors = ["Your Name <your@email.com>"]
readme = "README.md"
packages = [{include = "cognix"}]

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.7"
apscheduler = "^3.10.4"
lark-oapi = "^1.2.1"
python-dotenv = "^1.0.1"

[tool.poetry.scripts]
cognix = "cognix.interfaces.cli.main:cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
```

- [ ] **步骤 2：编写基础 README.md**

````markdown
# Cognix
面向企业 AI Agent 的个人工作习惯与偏好记忆引擎

## 安装
```bash
pip install cognix
````

## 快速开始

```bash
# 初始化配置
cognix init

# 查看帮助
cognix --help
```

````

- [ ] **步骤 3：编写配置管理工具类**
```python
# cognix/utils/config.py
import os
from pathlib import Path
from dotenv import load_dotenv

class Config:
    def __init__(self):
        self.home_path = Path.home() / ".cognix"
        self.home_path.mkdir(exist_ok=True)
        
        self.db_path = self.home_path / "cognix.db"
        self.events_path = self.home_path / "events"
        self.events_path.mkdir(exist_ok=True)
        
        load_dotenv(self.home_path / ".env")
        
        self.feishu_app_id = os.getenv("FEISHU_APP_ID", "")
        self.feishu_app_secret = os.getenv("FEISHU_APP_SECRET", "")
        self.llm_base_url = os.getenv("LLM_BASE_URL", "")
        self.llm_api_key = os.getenv("LLM_API_KEY", "")
        self.llm_model = os.getenv("LLM_MODEL", "qwen3.5-35b")

config = Config()
````

- [ ] **步骤 4：初始化项目依赖**
  运行：`poetry install`
  预期：依赖安装成功，虚拟环境创建完成
- [ ] **步骤 5：Commit**

```bash
git add pyproject.toml README.md cognix/__init__.py cognix/utils/config.py
git commit -m "feat: init project structure and config"
```

***

### 任务 2：存储层实现

**文件：**

- 创建：`cognix/storage/sqlite_store.py`
- 创建：`cognix/storage/file_store.py`
- [ ] **步骤 1：编写 SQLite 存储实现**

```python
# cognix/storage/sqlite_store.py
import sqlite3
from typing import Dict, List, Optional
from cognix.utils.config import config
import json

class SQLiteStore:
    def __init__(self):
        self.conn = sqlite3.connect(config.db_path)
        self._init_tables()
    
    def _init_tables(self):
        cursor = self.conn.cursor()
        
        # 偏好表
        cursor.execute('''
        CREATE TABLE IF NOT EXISTS preferences (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            key TEXT UNIQUE NOT NULL,
            value TEXT NOT NULL,
            weight REAL DEFAULT 1.0,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )
        ''')
        
        # 规则表
        cursor.execute('''
        CREATE TABLE IF NOT EXISTS rules (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            name TEXT NOT NULL,
            trigger TEXT NOT NULL,
            action TEXT NOT NULL,
            status TEXT DEFAULT 'pending',  # pending/active/disabled
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )
        ''')
        
        self.conn.commit()
    
    # 偏好相关操作
    def set_preference(self, key: str, value: Dict, weight: float = 1.0):
        cursor = self.conn.cursor()
        cursor.execute('''
        REPLACE INTO preferences (key, value, weight, updated_at)
        VALUES (?, ?, ?, CURRENT_TIMESTAMP)
        ''', (key, json.dumps(value), weight))
        self.conn.commit()
    
    def get_preference(self, key: str) -> Optional[Dict]:
        cursor = self.conn.cursor()
        cursor.execute('SELECT value FROM preferences WHERE key = ?', (key,))
        result = cursor.fetchone()
        return json.loads(result[0]) if result else None
    
    def list_preferences(self) -> List[Dict]:
        cursor = self.conn.cursor()
        cursor.execute('SELECT key, value, weight FROM preferences')
        return [{"key": row[0], "value": json.loads(row[1]), "weight": row[2]} for row in cursor.fetchall()]
    
    # 规则相关操作
    def add_rule(self, name: str, trigger: str, action: Dict, status: str = 'pending') -> int:
        cursor = self.conn.cursor()
        cursor.execute('''
        INSERT INTO rules (name, trigger, action, status)
        VALUES (?, ?, ?, ?)
        ''', (name, trigger, json.dumps(action), status))
        self.conn.commit()
        return cursor.lastrowid
    
    def update_rule_status(self, rule_id: int, status: str):
        cursor = self.conn.cursor()
        cursor.execute('''
        UPDATE rules SET status = ?, updated_at = CURRENT_TIMESTAMP
        WHERE id = ?
        ''', (status, rule_id))
        self.conn.commit()
    
    def list_rules(self, status: Optional[str] = None) -> List[Dict]:
        cursor = self.conn.cursor()
        if status:
            cursor.execute('SELECT id, name, trigger, action, status FROM rules WHERE status = ?', (status,))
        else:
            cursor.execute('SELECT id, name, trigger, action, status FROM rules')
        return [
            {"id": row[0], "name": row[1], "trigger": row[2], "action": json.loads(row[3]), "status": row[4]} 
            for row in cursor.fetchall()
        ]
    
    def close(self):
        self.conn.close()

sqlite_store = SQLiteStore()
```

- [ ] **步骤 2：编写文件存储实现（行为日志）**

```python
# cognix/storage/file_store.py
import json
from pathlib import Path
from datetime import datetime
from typing import List, Dict
from cognix.utils.config import config

class FileStore:
    def __init__(self):
        self.base_path = config.events_path
    
    def add_event(self, event_type: str, data: Dict):
        today = datetime.now().strftime("%Y-%m-%d")
        file_path = self.base_path / f"{today}.jsonl"
        
        event = {
            "timestamp": datetime.now().isoformat(),
            "type": event_type,
            "data": data
        }
        
        with open(file_path, "a", encoding="utf-8") as f:
            f.write(json.dumps(event, ensure_ascii=False) + "\n")
    
    def get_events(self, start_date: Optional[str] = None, end_date: Optional[str] = None, event_type: Optional[str] = None) -> List[Dict]:
        events = []
        for file in self.base_path.glob("*.jsonl"):
            date_str = file.stem
            if start_date and date_str < start_date:
                continue
            if end_date and date_str > end_date:
                continue
            
            with open(file, "r", encoding="utf-8") as f:
                for line in f:
                    try:
                        event = json.loads(line.strip())
                        if event_type and event["type"] != event_type:
                            continue
                        events.append(event)
                    except:
                        continue
        
        return sorted(events, key=lambda x: x["timestamp"])

file_store = FileStore()
```

- [ ] **步骤 3：验证存储功能**
  运行：`poetry run python -c "
  from cognix.storage.sqlite_store import sqlite_store
  sqlite_store.set_preference('test', {'value': 'test'})
  print(sqlite_store.get_preference('test'))
  from cognix.storage.file_store import file_store
  file_store.add_event('test', {'content': 'test event'})
  print(file_store.get_events())
  "`
  预期：输出测试偏好和测试事件，无报错
- [ ] **步骤 4：Commit**

```bash
git add cognix/storage/sqlite_store.py cognix/storage/file_store.py
git commit -m "feat: implement storage layer (sqlite + file)"
```

***

### 任务 3：核心模块实现 - 偏好存储与规则引擎

**文件：**

- 创建：`cognix/core/preference_store.py`
- 创建：`cognix/core/rule_engine.py`
- [ ] **步骤 1：编写偏好存储模块**

```python
# cognix/core/preference_store.py
from typing import Dict, Optional, List
from cognix.storage.sqlite_store import sqlite_store

class PreferenceStore:
    def __init__(self):
        self.store = sqlite_store
    
    def set(self, key: str, value: Dict, weight: float = 1.0):
        """设置用户偏好"""
        self.store.set_preference(key, value, weight)
    
    def get(self, key: str) -> Optional[Dict]:
        """获取用户偏好"""
        return self.store.get_preference(key)
    
    def list(self) -> List[Dict]:
        """列出所有偏好"""
        return self.store.list_preferences()
    
    def get_report_format(self) -> str:
        """获取报告输出格式，默认 table"""
        pref = self.get("report_format")
        return pref.get("value", "table") if pref else "table"
    
    def get_weekly_report_receiver(self) -> str:
        """获取周报接收人"""
        pref = self.get("weekly_report_receiver")
        return pref.get("value", "") if pref else ""
    
    def get_meeting_reminder_time(self) -> int:
        """获取会议提醒提前时间（分钟），默认 5 分钟"""
        pref = self.get("meeting_reminder")
        return pref.get("value", 5) if pref else 5

preference_store = PreferenceStore()
```

- [ ] **步骤 2：编写规则引擎模块**

```python
# cognix/core/rule_engine.py
from typing import Dict, List, Optional
from cognix.storage.sqlite_store import sqlite_store
import json

class RuleEngine:
    def __init__(self):
        self.store = sqlite_store
    
    def create_rule(self, name: str, trigger: str, action: Dict) -> int:
        """创建新规则，默认待确认状态"""
        return self.store.add_rule(name, trigger, action, status="pending")
    
    def confirm_rule(self, rule_id: int):
        """确认规则，激活生效"""
        self.store.update_rule_status(rule_id, "active")
    
    def reject_rule(self, rule_id: int):
        """拒绝规则，禁用"""
        self.store.update_rule_status(rule_id, "disabled")
    
    def get_active_rules(self) -> List[Dict]:
        """获取所有激活的规则"""
        return self.store.list_rules(status="active")
    
    def get_pending_rules(self) -> List[Dict]:
        """获取待确认的规则"""
        return self.store.list_rules(status="pending")
    
    def generate_weekly_report_rule(self, time: str = "15:55", day_of_week: str = "fri") -> int:
        """生成周报规则"""
        action = {
            "type": "weekly_report",
            "format": preference_store.get_report_format(),
            "receiver": preference_store.get_weekly_report_receiver()
        }
        trigger = f"cron: {time} * * {day_of_week}"
        return self.create_rule("每周周报提醒", trigger, action)

rule_engine = RuleEngine()
```

- [ ] **步骤 3：验证核心模块功能**
  运行：`poetry run python -c "
  from cognix.core.preference_store import preference_store
  preference_store.set('report_format', {'value': 'table'})
  print(preference_store.get_report_format())
  from cognix.core.rule_engine import rule_engine
  rule_id = rule_engine.generate_weekly_report_rule()
  print(rule_engine.get_pending_rules())
  rule_engine.confirm_rule(rule_id)
  print(rule_engine.get_active_rules())
  "`
  预期：输出表格格式、待确认规则、激活规则，无报错
- [ ] **步骤 4：Commit**

```bash
git add cognix/core/preference_store.py cognix/core/rule_engine.py
git commit -m "feat: implement preference store and rule engine"
```

***

### 任务 4：核心模块实现 - 调度器与事件采集

**文件：**

- 创建：`cognix/core/scheduler.py`
- 创建：`cognix/core/event_collector.py`
- [ ] **步骤 1：编写调度器模块**

```python
# cognix/core/scheduler.py
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger
from typing import Callable
import re
from cognix.core.rule_engine import rule_engine

class Scheduler:
    def __init__(self):
        self.scheduler = BackgroundScheduler()
        self.action_handlers = {}
        self._load_active_rules()
    
    def register_action_handler(self, action_type: str, handler: Callable):
        """注册动作处理器"""
        self.action_handlers[action_type] = handler
    
    def _parse_trigger(self, trigger_str: str):
        """解析触发表达式，支持 cron 格式"""
        if trigger_str.startswith("cron: "):
            cron_expr = trigger_str[6:].strip()
            parts = cron_expr.split()
            if len(parts) == 5:
                return CronTrigger(minute=parts[0], hour=parts[1], day=parts[2], month=parts[3], day_of_week=parts[4])
        return None
    
    def _execute_action(self, action: Dict):
        """执行动作"""
        action_type = action.get("type")
        if action_type in self.action_handlers:
            self.action_handlers[action_type](action)
    
    def _load_active_rules(self):
        """加载所有激活的规则"""
        rules = rule_engine.get_active_rules()
        for rule in rules:
            trigger = self._parse_trigger(rule["trigger"])
            if trigger:
                self.scheduler.add_job(
                    self._execute_action,
                    trigger=trigger,
                    args=[rule["action"]],
                    id=f"rule_{rule['id']}",
                    replace_existing=True
                )
    
    def start(self):
        """启动调度器"""
        self.scheduler.start()
    
    def shutdown(self):
        """关闭调度器"""
        self.scheduler.shutdown()
    
    def reload_rules(self):
        """重新加载规则"""
        self.scheduler.remove_all_jobs()
        self._load_active_rules()

scheduler = Scheduler()
```

- [ ] **步骤 2：编写事件采集模块**

```python
# cognix/core/event_collector.py
from typing import Dict
from cognix.storage.file_store import file_store

class EventCollector:
    def __init__(self):
        self.store = file_store
    
    def track_cli_command(self, command: str, args: Dict = None):
        """跟踪 CLI 命令执行"""
        event_data = {
            "command": command,
            "args": args or {}
        }
        self.store.add_event("cli_command", event_data)
    
    def track_feishu_interaction(self, event_type: str, data: Dict):
        """跟踪飞书交互事件"""
        self.store.add_event("feishu_interaction", {"event_type": event_type, **data})
    
    def track_user_feedback(self, rule_id: int, action: str, feedback: str = ""):
        """跟踪用户反馈（确认/拒绝规则等）"""
        event_data = {
            "rule_id": rule_id,
            "action": action,
            "feedback": feedback
        }
        self.store.add_event("user_feedback", event_data)
    
    def track_preference_change(self, key: str, old_value: Dict, new_value: Dict):
        """跟踪偏好变更"""
        event_data = {
            "key": key,
            "old_value": old_value,
            "new_value": new_value
        }
        self.store.add_event("preference_change", event_data)

event_collector = EventCollector()
```

- [ ] **步骤 3：验证调度器功能**
  运行：`poetry run python -c "
  from cognix.core.scheduler import scheduler
  def test_handler(action):
    print(f'执行动作：{action}')
  scheduler.register_action_handler('weekly_report', test_handler)
  scheduler.start()
  print('调度器启动成功')
  scheduler.shutdown()
  "`
  预期：输出调度器启动成功，无报错
- [ ] **步骤 4：Commit**

```bash
git add cognix/core/scheduler.py cognix/core/event_collector.py
git commit -m "feat: implement scheduler and event collector"
```

***

### 任务 5：CLI 工具实现

**文件：**

- 创建：`cognix/interfaces/cli/main.py`
- [ ] **步骤 1：编写 CLI 入口代码**

```python
# cognix/interfaces/cli/main.py
import click
from cognix.utils.config import config
from cognix.core.preference_store import preference_store
from cognix.core.rule_engine import rule_engine
from cognix.core.scheduler import scheduler
from cognix.core.event_collector import event_collector
import json
import time

@click.group()
def cli():
    """Cognix：个人工作习惯与偏好记忆引擎"""
    pass

@cli.command()
def init():
    """初始化 Cognix 配置"""
    click.echo("初始化 Cognix 配置...")
    config_path = config.home_path / ".env"
    if not config_path.exists():
        with open(config_path, "w", encoding="utf-8") as f:
            f.write("""# 飞书配置
FEISHU_APP_ID=
FEISHU_APP_SECRET=

# LLM 配置
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=qwen3.5-35b
""")
    click.echo(f"配置文件已生成：{config_path}")
    click.echo("请编辑配置文件后开始使用")

@cli.group()
def config():
    """偏好配置管理"""
    pass

@config.command("set")
@click.argument("key")
@click.argument("value")
def set_config(key, value):
    """设置偏好：cognix config set report_format table"""
    try:
        value_dict = json.loads(value) if value.startswith("{") else {"value": value}
    except:
        value_dict = {"value": value}
    
    old_value = preference_store.get(key)
    preference_store.set(key, value_dict)
    event_collector.track_preference_change(key, old_value, value_dict)
    click.echo(f"已设置偏好：{key} = {value}")

@config.command("list")
def list_config():
    """列出所有偏好"""
    prefs = preference_store.list()
    for pref in prefs:
        click.echo(f"{pref['key']}: {json.dumps(pref['value'], ensure_ascii=False)} (权重: {pref['weight']})")

@cli.group()
def rule():
    """规则管理"""
    pass

@rule.command("list")
@click.option("--status", default=None, help="规则状态：pending/active/disabled")
def list_rules(status):
    """列出所有规则"""
    rules = rule_engine.store.list_rules(status)
    for rule in rules:
        click.echo(f"[{rule['id']}] {rule['name']} - {rule['status']}")
        click.echo(f"  触发条件: {rule['trigger']}")
        click.echo(f"  执行动作: {json.dumps(rule['action'], ensure_ascii=False)}")
        click.echo()

@rule.command("confirm")
@click.argument("rule_id", type=int)
def confirm_rule(rule_id):
    """确认待激活的规则"""
    rule_engine.confirm_rule(rule_id)
    scheduler.reload_rules()
    event_collector.track_user_feedback(rule_id, "confirm")
    click.echo(f"规则 {rule_id} 已激活")

@rule.command("reject")
@click.argument("rule_id", type=int)
def reject_rule(rule_id):
    """拒绝待激活的规则"""
    rule_engine.reject_rule(rule_id)
    event_collector.track_user_feedback(rule_id, "reject")
    click.echo(f"规则 {rule_id} 已禁用")

@rule.command("create-weekly")
@click.option("--time", default="15:55", help="触发时间，默认 15:55")
@click.option("--day", default="fri", help="触发星期，默认周五（fri）")
def create_weekly_rule(time, day):
    """创建周报规则"""
    rule_id = rule_engine.generate_weekly_report_rule(time, day)
    click.echo(f"周报规则已创建，ID：{rule_id}")
    click.echo("请运行 cognix rule confirm 激活规则")

@cli.command()
def weekly_report():
    """手动生成周报"""
    event_collector.track_cli_command("weekly_report")
    fmt = preference_store.get_report_format()
    receiver = preference_store.get_weekly_report_receiver()
    click.echo(f"正在生成 {fmt} 格式周报...")
    # 此处后续接入LLM生成逻辑
    click.echo(f"周报生成完成，将发送给：{receiver}")

@cli.command()
def start():
    """启动 Cognix 后台服务"""
    click.echo("启动 Cognix 服务...")
    
    # 注册动作处理器
    def handle_weekly_report(action):
        click.echo("\n" + "="*50)
        click.echo("🔔 周报提醒：")
        click.echo(f"已按你偏好的 {action['format']} 格式生成周报草稿")
        if action['receiver']:
            click.echo(f"默认接收人：{action['receiver']}")
        click.echo("是否发送？(y/n)")
        # 此处后续实现通知逻辑
    
    scheduler.register_action_handler("weekly_report", handle_weekly_report)
    
    scheduler.start()
    click.echo("服务已启动，按 Ctrl+C 停止")
    
    try:
        while True:
            time.sleep(1)
    except KeyboardInterrupt:
        scheduler.shutdown()
        click.echo("\n服务已停止")

if __name__ == "__main__":
    cli()
```

- [ ] **步骤 2：验证 CLI 功能**
  运行：`poetry run cognix --help`
  预期：显示 CLI 帮助信息，无报错

运行：`poetry run cognix init`
预期：生成 \~/.cognix 目录和配置文件

运行：`poetry run cognix config set report_format table`
预期：设置偏好成功

运行：`poetry run cognix config list`
预期：显示已设置的偏好

- [ ] **步骤 3：Commit**

```bash
git add cognix/interfaces/cli/main.py
git commit -m "feat: implement CLI tool"
```

***

### 任务 6：飞书 Bot 基础实现

**文件：**

- 创建：`cognix/interfaces/feishu/bot.py`
- [ ] **步骤 1：编写飞书 Bot 基础代码**

```python
# cognix/interfaces/feishu/bot.py
from lark_oapi import Client, JSON
from lark_oapi.api.im.v1 import *
from cognix.utils.config import config
from cognix.core.event_collector import event_collector
from cognix.core.rule_engine import rule_engine
from cognix.core.preference_store import preference_store

class FeishuBot:
    def __init__(self):
        self.client = Client.builder() \
            .app_id(config.feishu_app_id) \
            .app_secret(config.feishu_app_secret) \
            .build()
    
    def send_message(self, receive_id_type: str, receive_id: str, content: str, msg_type: str = "text"):
        """发送消息"""
        req = CreateMessageRequest.builder() \
            .receive_id_type(receive_id_type) \
            .request_body(CreateMessageRequestBody.builder()
                .receive_id(receive_id)
                .msg_type(msg_type)
                .content(JSON.dumps({"text": content}))
                .build()) \
            .build()
        
        resp = self.client.im.v1.message.create(req)
        return resp.success()
    
    def send_weekly_report_reminder(self, user_open_id: str, format: str, receiver: str):
        """发送周报提醒卡片"""
        content = JSON.dumps({
            "elements": [
                {
                    "tag": "div",
                    "text": {
                        "content": f"📅 今天是周五，已按你偏好的 {format} 格式生成本周周报草稿，是否发送给 {receiver}？",
                        "tag": "lark_md"
                    }
                },
                {
                    "tag": "action",
                    "actions": [
                        {
                            "tag": "button",
                            "text": {"content": "立即发送", "tag": "plain_text"},
                            "type": "primary",
                            "value": {"action": "confirm_weekly_report"}
                        },
                        {
                            "tag": "button",
                            "text": {"content": "稍后再说", "tag": "plain_text"},
                            "type": "default",
                            "value": {"action": "reject_weekly_report"}
                        }
                    ]
                }
            ]
        })
        
        req = CreateMessageRequest.builder() \
            .receive_id_type("open_id") \
            .request_body(CreateMessageRequestBody.builder()
                .receive_id(user_open_id)
                .msg_type("interactive")
                .content(content)
                .build()) \
            .build()
        
        resp = self.client.im.v1.message.create(req)
        return resp.success()
    
    def handle_event(self, event: Dict):
        """处理飞书事件"""
        event_type = event.get("header", {}).get("event_type")
        event_data = event.get("event", {})
        
        if event_type == "im.message.receive_v1":
            # 处理消息接收事件
            message = event_data.get("message", {})
            sender = event_data.get("sender", {})
            content = json.loads(message.get("content", "{}")).get("text", "")
            user_open_id = sender.get("sender_id", {}).get("open_id", "")
            
            event_collector.track_feishu_interaction("message_receive", {
                "user_open_id": user_open_id,
                "content": content
            })
            
            if "周报" in content:
                fmt = preference_store.get_report_format()
                receiver = preference_store.get_weekly_report_receiver()
                self.send_weekly_report_reminder(user_open_id, fmt, receiver)
        
        elif event_type == "im.message.action.triggered_v1":
            # 处理卡片按钮点击事件
            action = event_data.get("action", {})
            action_value = action.get("value", {})
            user_open_id = event_data.get("operator", {}).get("open_id", "")
            
            event_collector.track_feishu_interaction("action_click", {
                "user_open_id": user_open_id,
                "action": action_value
            })
            
            if action_value.get("action") == "confirm_weekly_report":
                self.send_message("open_id", user_open_id, "✅ 周报已发送")
            elif action_value.get("action") == "reject_weekly_report":
                self.send_message("open_id", user_open_id, "👌 已取消发送")

feishu_bot = FeishuBot()
```

- [ ] **步骤 2：验证飞书 Bot 功能**
  运行：`poetry run python -c "
  from cognix.interfaces.feishu.bot import feishu_bot
  print('飞书 Bot 初始化成功')
  "`
  预期：输出初始化成功，无报错
- [ ] **步骤 3：Commit**

```bash
git add cognix/interfaces/feishu/bot.py
git commit -m "feat: implement feishu bot basic functions"
```

***

### 任务 7：MVP 场景验证

**文件：**

- 测试用例执行
- [ ] **步骤 1：配置周报偏好**
  运行：`cognix config set report_format table`
  运行：`cognix config set weekly_report_receiver "Leader A"`
- [ ] **步骤 2：创建并激活周报规则**
  运行：`cognix rule create-weekly --time "* * * * *"`  # 测试用每分钟触发
  运行：`cognix rule confirm 1`
- [ ] **步骤 3：启动服务验证触发**
  运行：`cognix start`
  预期：每分钟收到一次周报提醒，格式正确，接收人正确
- [ ] **步骤 4：Commit 测试结果**

```bash
git add tests/
git commit -m "test: mvp scenario verification passed"
```

